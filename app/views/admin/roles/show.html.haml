%p
  %b
    = I18n.t('name').capitalize
    \:
  = h @role.name
%p
  %b
    = I18n.t('rights',:count => 2).capitalize
    \:
    %b
      %table
        - @role.rights.in_groups_of(4,false).each do |right_group|
          %tr
            - right_group.each do |right|
              %td{ :colspan => (4 / right_group.size)}
                = h right.name

- content_for :tools, link_to(I18n.t('edit').capitalize, [:edit, :admin, @role])
