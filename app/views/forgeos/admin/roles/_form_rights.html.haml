= hidden_field_tag "role[right_ids][]"
%table
  - rights.in_groups_of(4,false).each do |right_group|
    %tr
      - right_group.each do |right|
        %td.content
          = check_box_tag "role[right_ids][]", right.id, (role ? role.rights.include?(right) : false)
          = right.name
