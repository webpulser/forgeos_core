<%= dataTables_resultset(Forgeos::Right, @rights) do |right|
  [
    handler_icon('legal', right),
    content_tag(:div, right.name, :class => 'right-name'),
    right.controller_name,
    right.action_name,
    link_to('', '#', :class => 'icon icon-copy duplicate-right') +
    link_to('', '#', :class => 'icon icon-pencil edit-right') +
    destroy_button(forgeos_core, right)
  ]
end -%>
