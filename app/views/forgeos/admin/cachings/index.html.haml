= render :partial => 'tabs'

#content.grid_16.alpha.omega
  = form_for :caching, :url => [forgeos_core, :admin, :cachings] do |f|
    .backgrounds.header
      .interact-links
        = f.submit t('caching.delete.one'), :class => 'backgrounds interact-button'
        .left=t 'or'
        = f.submit t('caching.delete.all'), :class => 'backgrounds interact-button'
    .files
      #caching-tree
